# Build-time Gemini API key. Copy this file to ignored .env and set a real key.
# The Secrets Gradle Plugin packages the value into BuildConfig in the APK.
# Keep any APK containing a personal API key private.
# Without a key, Gemini chat reports an explicit configuration error.
GEMINI_API_KEY=MY_GEMINI_API_KEY
